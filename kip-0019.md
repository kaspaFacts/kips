```
  KIP: 19
  Layer: Applications
  Title: Proof of Inclusion
  Author: kaspaFacts
  Status: Draft
```

TODO acceptedidmerkleroot field is not sufficent for proof, we require the root of all txids in the mergeset(the actual
acceptedidmerkleroot before hashing with sp acceptedidmerkleroot to commit the seqcomm, the acceptedidmerkleroot field
committed to the header), (this is the sibling node for verifying a merkle proof)

TODO correct usage of terminology throughout

TODO ensure rest of kip matches outline in Supplementary information


# Motivation  

Proof of inclusion provides a mechanism for applications to verify that a specific transaction was included in the
Kaspa DAG without requiring access to the full blockchain history. This enables lightweight verification of transaction
status, audit trails, and proof of payment systems among many possible use-cases.

While KIP-15 provides sequencing commitments for comprehensive transaction indexing, this KIP focuses specifically on
efficient single-transaction inclusion proofs that users can generate and store independently.

Critically, anyone with this proof can then use data preserved by a Kaspa full node to verify inclusion, enabling
efficient verification without needing archival nodes or complete blockchain data. This approach leverages the existing
infrastructure of full nodes while providing cryptographic guarantees of transaction inclusion.

This document provides a standard for applications to generate, store, and verify inclusion proofs with cryptographic
security guarantees, particularly before relevant data is pruned from the network. The protocol defines the proof
structure, verification requirements, and interoperability standards to ensure compatibility across different Kaspa
applications and services. 
  
# Notations  
  
# Specifications  
  
## Proof Structure Overview  

KIP-19 defines a standardized format for cryptographic proofs that demonstrate transaction inclusion in the Kaspa DAG.
The proof system enables verification without requiring full blockchain access by leveraging preserved consensus data
and efficient cryptographic commitments.

### Standardized format for cryptographic proofs  
  
## Core Components  

### Transaction Data  

Full transaction bytes serialized according to the Kaspa transaction format, including all standard fields (version, inputs, outputs, lock time, subnetwork ID, gas, payload).

### Merkle Proof  

Path from transaction ID to the block's Accepted ID Merkle Root (ATMR):

- Uses **sorted Transaction IDs** from the accepting block's mergeset
- Transaction IDs are sorted before merkle root calculation
- Always use Transaction IDs, not full transaction hashes for proofs

### Clarification: SequencingCommitment and AcceptedIDMerkleRoot

**Note on Terminology**: Throughout this document, the term "SequencingCommitment" refers to the existing
`accepted_id_merkle_root` field in Kaspa block headers. SequencingCommitment is different than AcceptedIDMerkleRoot.
AcceptedIDMerkleRoot is calculated from Transaction ID's in the mergeset of the accepting block. AcceptedIDMerkleRoot is
then hashed with the current blocks SelectedParent.accepted_id_merkle_root field (The Sequencing Commitment) to generate
this blocks SequencingCommmitment.

## Data Formats  

### Serialization Order  

All multi-byte numeric fields use little-endian encoding except hashes which use natural byte order.

### Proof Structure  

- **version**: Protocol version number  
- **proof_type**: Single or batch proof indicator  
- **transactions**: Array of transaction proofs  
- **chain_proof**: SequencingCommitment chain data  
- **pruning_point**: Pruning point reference data  
  
# Pruning Point System  

The `PastPruningPointsStore` is an append-only database store that maintains a permanent, indexed record of all historical pruning points in Kaspa. Each entry represents a block selected as a pruning point and is preserved forever, even as other block data at those heights gets pruned.  
  
## Accepted ID Merkle Root Construction  

Kaspa creates each block's `accepted_id_merkle_root` by combining:  
1. **Selected parent's accepted ID merkle root** - Retrieved from the parent block header  
2. **Current block's accepted transaction IDs merkle root** - Calculated from sorted transaction IDs in the current block's mergeset

## Chain Verification Entry Point  

The accepting block's selected parent `accepted_id_merkle_root` serves as the entry point for chain verification because:

- Each block's `accepted_id_merkle_root` incorporates its selected parent's `accepted_id_merkle_root`
- This creates a verifiable chain of commitments from any block back to the pruning point
- Walking this chain proves the accepting block is in the selected chain without requiring full blockchain history

The verification process walks the `accepted_id_merkle_root` chain from the accepting block to the pruning point, using preserved block headers stored by full nodes.

## Index-Based Organization  

The store uses a simple index-to-hash mapping structure for O(1) lookup performance:  
- **Index 0**: Always contains the genesis block hash  
- **Sequential indexing**: Each new pruning point receives the next available index (1, 2, 3, ...)  
- **Monotonic growth**: Indices never decrease or are reused  
  
This indexing dramatically improves verification performance for historical proofs and enables efficient range queries and batch verification.  

## Block Entry Criteria  

A block enters the PastPruningPointsStore when it satisfies these conditions:  
  
1. **Pruning Sample Eligibility**: `finality_score(block) > finality_score(selected_parent)` where `finality_score = blue_score / finality_depth`  
2. **Sufficient Depth**: At least `pruning_depth` blocks behind the current tip/virtual state  
3. **Chain Selection**: On the selected chain from the current tip  
4. **Finalization**: Achieved sufficient burial depth to mitigate reorg risks  

## Storage Mechanisms  

Blocks are added through three primary mechanisms:  
- **Normal Pruning Point Advancement**: When the network advances and identifies new pruning points  
- **Genesis Initialization**: At node startup, genesis is stored at index 0  
- **IBD/Catchup Sync**: When importing pruning points during initial block download  

## Preservation Guarantees  

Once stored, pruning points are preserved permanently:  
- Headers are never pruned from these indices  
- The store is append-only with no deletion mechanism  
- This enables long-term verification of historical inclusion proofs  

## Pruning Point Data  

Each stored pruning point contains:  
- **Header ID** (hash) for search optimization  
- **SequencingCommitment** of the pruning point  
- Full block header for complete verification  
  
The PastPruningPointsStore serves as the foundation for KIP-19 verification, allowing proofs to anchor to any historical pruning point while maintaining verifiability without requiring full archival nodes.
  
# Chain Verification  

Verify the accepting block is in the selected chain by walking the SequencingCommitment chain from the selected parent to the pruning point. Each SequencingCommitment is calculated as `hash(SelectedParent.SequencingCommitment, AcceptedIDMerkleRoot)`.  
  
## Verification Steps  

1. Start from the accepting block's selected parent SequencingCommitment  
2. Walk forward through the chain of commitments  
3. Each step verifies: `current_seqcommit = hash(prev_seqcommit, block_accepted_id_merkle_root)`  
4. Continue until reaching the pruning point's SequencingCommitment  
5. Verify the pruning point exists in PastPruningPointsStore
  
# Temporal Verification  

Inclusion proofs provide three levels of temporal guarantees for verifying when a transaction occurred.
  
Applications SHOULD document whether they provide temporal estimates tighter than the 12-hour pruning point window.
 
## Rough Time Zone  

Inclusion proof verifies inclusion between pruning points (~12-hour window). Requires only pruning
point index.

## Estimated Wall Clock Time  

Estimate timestamp based on ATMR count between transaction block and pruning point, using average block rate and DAG
width assumptions. Requires no additional preserved data.

## Estimated DAA Time  

Estimate DAA score based on ATMR count between transaction block and pruning point, using the same block rate and width
assumptions. Requires no additional preserved data.

## Security Note  

Full block headers cannot provide guaranteed timestamp or DAA accuracy without preserving all intermediate headers for
verification. Both wall clock and DAA time estimation use the same secure ATMR-based approach, leveraging the
cryptographic commitment chain for temporal accuracy.

# Proof Generation Timing TODO clarify this section to focus on incremental proof generation AND clarify that once pp_candidates + 2 are identified, the proof you generated is deterministically final
  
Applications can begin generating proofs immediately after transaction acceptance, using incremental verification to ensure the chain remains stable. Proof generation involves fetching AcceptedIDMerkleRoot values for blocks between the accepting block and the next pruning point, with API calls limited by mergeset_size_limit.  
  
Applications MUST document their finality strategy and clearly communicate to users whether proofs are generated with deterministic or probabilistic guarantees regarding preservation at a permanent pruning point.  
  
## Incremental Proof Generation with Chain Verification    
  
Applications can generate proofs incrementally by:  
  
1. **Early Start**: Begin proof generation immediately after transaction acceptance  
2. **Batch Collection**: Fetch AcceptedIDMerkleRoot values in batches limited by mergeset_size_limit  
3. **Chain Verification**: After each batch, verify chain continuity by:  
   - Taking the last collected AcceptedIDMerkleRoot  
   - Hashing it with the next block's parent SequencingCommitment  
   - Verifying the result matches the next block's header SequencingCommitment field  
   - This confirms the previous block remains in the selected chain  
4. **Continue Progression**: If verification succeeds, request the next batch of accepted transaction IDs  
5. **Complete to Pruning Point**: Continue until reaching the next pruning point for permanent verifiability  
  
The verification step uses the same calculation as consensus: `hash(parent.SequencingCommitment, current.AcceptedIDMerkleRoot) == current.SequencingCommitment` [1](#7-0) .  
  
## Deterministic Finality    
  
Wait until two pruning epochs have passed since the accepting block. This occurs when:  
- Two subsequent pruning point candidates have been identified after the accepting block  
- The target pruning point is buried under finality depth  
- The proof points to a permanently preserved pruning point with zero reorg risk  
  
## Probabilistic Finality    
  
Wait until one pruning epoch has passed since the accepting block (next pruning point identified) plus desired confirmation depth. This provides:  
- High confidence against reorgs (probability decreases with each pruning epoch)  
- Faster proof generation  
- Small remaining risk of chain reorganization invalidating the proof  
  
## Pruning Epoch Timing    
  
Based on network parameters:  
- Pruning epochs occur approximately every finality_depth blocks (432,000 blocks at 10 BPS)  
- Each epoch represents ~12 hours of block time  
- Two epochs (~24 hours) provide deterministic finality for proof generation

# Cryptographic Commitments in Block Headers    
  
KIP-19 leverages Kaspa's block header commitments for secure inclusion proofs. While headers contain 13 fields, only the  
`accepted_id_merkle_root` (SequencingCommitment) is used as verification endpoints in KIP-19 proofs.  
  
## Security Model    
  
Any header field can contain arbitrary data with valid PoW. Without preserving the complete header chain to a permanent  
pruning point, such data cannot be verified and could be forged. KIP-19 achieves security by preserving the  
AcceptedIDMerkleRoot (transaction ID merkle roots) for every block from the transaction block to a permanent pruning  
point, creating a verifiable cryptographic commitment that proves transaction data authenticity.  
  
## Key Fields    
  
- **Accepting block's selected parent `accepted_id_merkle_root`**: Starting point for chain verification  
- **Pruning point's `accepted_id_merkle_root`**: Final verification anchor  
- **Preserved AcceptedIDMerkleRoot values**: Intermediate transaction ID merkle roots for each block in the chain

# Verification Process  
  
## Required Data  
  
- **ProofOfInclusion**: Complete proof structure containing transaction bytes, tx_inclusion_proof, chain_proof_to_pruning_point, and supporting fields  
- **Pruning point header**: Retrieved from PastPruningPointsStore using future_pruning_point_index  
  
## Verification Steps  
  
1. **Transaction verification**: Hash `transaction_bytes` to verify it matches the first hash in `tx_inclusion_proof`  
  
2. **Transaction inclusion verification**: Use `tx_inclusion_proof` to verify the transaction ID is included in the virtual accepting block's AcceptedIDMerkleRoot  
  
3. **Chain commitment verification**: Combine the calculated AcceptedIDMerkleRoot with the virtual accepting block's selected parent SequencingCommitment using merkle hashing  
  
4. **Chain proof verification**: Use `chain_proof_to_pruning_point` to verify the combined hash commits to the pruning point's SequencingCommitment  
  
5. **Final validation**: Verify the result matches the pruning point header data retrieved via `future_pruning_point_index`

## Optimization Notes  
  
- The `future_pruning_point_index` enables O(1) lookup in PastPruningPointsStore
- Checksum fields (`accepting_block_accepted_id_merkle_root`, `future_pruning_point_sequencing_commitment`) provide quick validation without full verification  
- The two-stage proof separates small transaction verification from potentially large chain verification

# Multi-Transaction Support  
  
## Batch Proof Structure  

Single proof can contain multiple transactions spanning multiple blocks:  
  
- **Shared chain components**: Single SequencingCommitment proof from oldest transaction's block to pruning point  
- **Individual transaction data**: Full transaction bytes for each transaction  
- **Individual merkle proofs**: Separate proofs for each transaction to its respective ATMR  

## Size Efficiency  

Batch proofs provide significant efficiency by amortizing the chain verification cost and chain proof generation cost
across multiple transactions. The first transaction requires the full chain proof, while additional transactions only
need their individual merkle proofs and transaction data.  

## Branch Trimming  TODO determine if sample code is required for trimming

Proofs support dynamic removal of individual transaction proofs:  
  
- Remove transaction data and merkle proof for specific transactions  
- Preserve shared chain components for remaining transactions  
- Enables efficient proof size reduction when individual transactions are no longer needed  
  
# Hash Function Specifications  

## Algorithm  

Blake2b with 32-byte digests

## Domain Separation  

Uses `TransactionID` and `MerkleBranchHash` keys for inclusion proofs

## Encoding  

Little-endian for numeric fields, natural byte order for hashes
  
# Sample Implementation  TODO complete this entire section

This section (will provide) provides working code examples demonstrating KIP-19 inclusion proof generation and verification:  
  
- **Transaction serialization functions** for recreating transaction IDs  
- **Merkle proof generation** from transaction IDs to block ATMR  
- **SequencingCommitment verification** demonstrating chain verification  
- **Complete end-to-end workflow** from proof generation to verification  
  
The sample implementation in Python serves as a reference for developers implementing KIP-19 in other languages.
Application developers are responsible for creating production-ready implementations tailored to their specific use
cases and security requirements.
  
## Transaction serialization functions  
## Merkle proof generation  
## SequencingCommitment verification  
## Complete end-to-end workflow  
  
# Test Cases  TODO complete this entire section

Basic test cases to verify the sample implementation works correctly:  
  
- **Single transaction proof** generation and verification  
- **Multi-transaction batch proof** with shared chain components  
- **Branch trimming** functionality  
- **Temporal estimation** using ATMR-based methods  
  
These test cases demonstrate the proof format works as specified. Comprehensive testing is the responsibility of
implementation developers.
  
## Single transaction proof  
## Multi-transaction batch proof  
## Branch trimming  
## Temporal estimation  
  
# Security Considerations  
 
## Proof Forgery Resistance  

Merkle proofs provide logarithmic security per tree level - forging requires finding hash collisions in the Blake2b hash
function.

## Domain Separation Security  

Blake2b domain separators (`TransactionID`, `MerkleBranchHash`) prevent cross-protocol attacks between signing and proof
systems.

## Chain Verification Attacks  

The chain proof verifies both the AcceptedIDMerkleRoot calculation (derived from transaction IDs) and the
SequencingCommitment chain through merkle hashing. Since each block's SequencingCommitment is cryptographically
committed by combining the selected parent's SequencingCommitment with the current block's AcceptedIDMerkleRoot, the
entire chain cannot be forged without breaking the underlying Blake2b hash function.

## Privacy Implications  

Inclusion proofs reveal transaction output scripts and amounts since these are included in the transaction ID
calculation, but input signature scripts remain private as they are excluded from the txid. While full transaction bytes
are required for verification, the merkle proof only exposes the spending conditions and values, not the actual
authorization data.
  
# Resource Analysis  
  
## Proof Size Estimates  

Chain proof dominates total proof size, with merkle proof adding minimal overhead. Exact sizes vary based on network
conditions and implementation choices.

## Proof Size Considerations  

Chain proofs have a minimum size bound equal to the distance from the accepting block to the next pruning point, which
cannot be reduced through technical optimizations. The only strategy to create smaller proofs is timing transaction
inclusion to occur closer to the next pruning point by calculating when the next finality epoch will rollover. Since
each proof requires fetching all transaction IDs from every block in this range via sequential API requests limited by
mergeset_size_limit, both intentional bandwidth DoS attacks and unintentional resource exhaustion remain concerns when
multiple applications simultaneously request proofs. Implementations should consider request rate limiting and
validation costs to prevent resource exhaustion.

# Backwards Compatibility

This KIP introduces a new optional proof system without modifying existing consensus rules, block validation logic, or
network protocols. All existing node software and clients will continue to operate without any changes.

# Appendix  
  
## Supplementary Information  

### Required Preserved Information

#### Proof Structure
```
  
@dataclass  
class TransactionProofNode:  
   """Merkle proof node with direction tracking (for tx inclusion up to accepting_block.accepted_id_merkle_root)"""  
   hash: bytes  # 32 bytes
    
   is_right: bool  # Whether this hash is on the right side  
  
@dataclass  
class ChainProofNode:  
   """Merkle proof node without direction (for chain proof - space optimized - every chain proof calculates the left
   node from accepting_block.sequencing_commitment to future_pruning_point)"""  
    
   hash: bytes  # 32 bytes
  
@dataclass  
class ProofOfInclusion:  
   """Optimized proof structure for transaction inclusion in Kaspa"""  

   # Core proof data  
   tx_inclusion_proof: List[TransactionProofNode]  # [tx_to_prove_tx_id, sibling_node, sibling_node,...,sibling_node]  
   accepting_block_sp_sequencing_commitment: bytes  # 32 bytes  
   chain_proof_to_pruning_point: List[ChainProofNode]  # [accepting_block_accepted_id_merkle_root, accepting_block_selected_child_accepted_id_merkle_root, accepting_block_selected_child_accepted_id_merkle_root,...,pruning_point_accepted_id_merkle_root]  
   transaction_bytes: bytes  # Full transaction to prove, hashing this to tx_inclusion_proof[0].hash ensures tx data matches (checksum when not proving, proof when proving)  
     
   # Checksum/validation fields  
   accepting_block_accepted_id_merkle_root: bytes # 32 bytes  (checksum only, this is NOT a required component of proving since this is calculated from tx_inclusion_proof)  
   future_pruning_point_sequencing_commitment: bytes  # 32 bytes  (checksum only, this is NOT a required component of proving since future_pruning_point_sequencing_commitment is preserved forever)  
     
   # Metadata  
   future_pruning_point_index: int  # Index of the next pruning point moving forward from tx_to_prove, for lookup in PastPruningPointsStore

   # Workflow = transaction_bytes -> tx_to_prove_tx_id -> accepting_block_accepted_id_merkle_root -> future_pruning_point_sequencing_commitment

```


#### Transaction-Specific Data.

1. Complete transaction bytes for the target transaction (TX_BLOCK.TX)
2. Merkle proof from the transaction ID to the accepting block's AcceptedIDMerkleRoot

#### Block Chain Data

For every block from the accepting block (TX_BLOCK) to the next permanent pruning point (P):

1. AcceptedIDMerkleRoot for every block from TX_BLOCK to P
2. SequencingCommitment for TX_BLOCK's Selected Parent

#### Pruning Point Reference

1. Pruning Point Index (for O(1) lookup/retrieval) in PastPruningPointStore
2. Pruning_Point.AcceptedIDMerkleRoot

### Transaction Inclusion Verification

1. **Initialize**:   
   - Let TX_BLOCK be the block accepting the transaction we are proving
   - Hash TX_BLOCK.TX to get TX_BLOCK.TX.TXID  
  
2. **Verify TXID in AcceptedIDMerkleRoot**:   
   - Verify Merkle Proof from TX_BLOCK.TX.TXID to TX_BLOCK.AcceptedIDMerkleRoot  
  
3. **For each block from TX_BLOCK to pruning point P**:  
   a. Get AcceptedIDMerkleRoot:  
      - If current block is TX_BLOCK: use TX_BLOCK.AcceptedIDMerkleRoot  
      - Otherwise: retrieve preserved AcceptedIDMerkleRoot  
   b. Get parent's SequencingCommitment: retrieve preserved current_block.SelectedParent.SequencingCommitment  
   c. Verify commitment:  
      ```  
      hash(current_block.SelectedParent.SequencingCommitment, current_block.AcceptedIDMerkleRoot) == current_block.SequencingCommitment  
      ```  
   d. Move to next block in chain: current_block = current_block.SelectedChild  
  
4. **Final verification**: Ensure preserved P.AcceptedIDMerkleRoot hashes to recognized pruning point P.SequencingCommitment

### Potential Optimizations for Nodes

The current chain proof generation is fundamentally inefficient because it requires retrieving all accepted transaction
IDs for every block along the selected parent chain path from the accepting block to the pruning point, then locally
calculating each AcceptedIDMerkleRoot. Since nodes only return transaction ID lists via the current API and don't cache
the pre-calculated AcceptedIDMerkleRoot values (even though they're computed once when creating the sequencing
commitment), each proof must reconstruct these roots by fetching the complete mergeset transaction IDs in order of
inclusion for every chain block. This creates unnecessary data transmission and computational overhead, as the same
calculations are repeated locally for what could be directly available as cached values, however evaluating the
cost/savings is out of scope for kip19